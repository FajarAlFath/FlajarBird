import 'package:flutter/material.dart';

void main() {
  runApp(const Flajar());
}

class Flajar extends StatelessWidget {
  const Flajar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
